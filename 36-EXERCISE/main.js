function makeShirt(size, msg) {
    console.log(`the size of the shirt is ${size} and the msg on the shirt is ${msg}`);
}
makeShirt("small", "believe");
makeShirt("medium", "perfect");
makeShirt("small", "yet to come");
export {};
